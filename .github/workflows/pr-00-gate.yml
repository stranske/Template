# -----------------------------------------------------------------------------
# Gate Workflow — Template Consumer Repo
# -----------------------------------------------------------------------------
# Orchestrates CI enforcement for pull requests. The `summary` job aggregates
# results and publishes the commit status that keepalive automation relies on.
# -----------------------------------------------------------------------------

name: Gate

on:
  pull_request:
    types: [opened, synchronize, reopened, ready_for_review, labeled]
  workflow_dispatch:

concurrency:
  group: pr-${{ github.event.pull_request.number || github.ref_name }}-gate
  cancel-in-progress: true

jobs:
  python-ci:
    name: Python CI
    uses: stranske/Workflows/.github/workflows/reusable-10-ci-python.yml@main
    with:
      python-versions: '["3.11", "3.12", "3.13"]'
      coverage-min: "80"
      format_check: false
      working-directory: "."
      artifact-prefix: "gate-"
    secrets: inherit

  summary:
    name: summary
    needs: [python-ci]
    if: always()
    runs-on: ubuntu-latest
    permissions:
      contents: read
      pull-requests: write
      statuses: write
    outputs:
      state: ${{ steps.summarize.outputs.state }}
      description: ${{ steps.summarize.outputs.description }}
    steps:
      - name: Summarize results
        id: summarize
        env:
          PYTHON_RESULT: ${{ needs.python-ci.result || 'skipped' }}
        run: |
          set -euo pipefail
          python_result="${PYTHON_RESULT:-skipped}"
          case "$python_result" in
            success) state="success"; description="Python CI passed" ;;
            failure) state="failure"; description="Python CI failed" ;;
            cancelled) state="failure"; description="Python CI was cancelled" ;;
            *) state="success"; description="Python CI was skipped" ;;
          esac
          echo "state=${state}" >> "$GITHUB_OUTPUT"
          echo "description=${description}" >> "$GITHUB_OUTPUT"
          echo "[Gate] State: ${state}, Description: ${description}"

      - name: Report Gate commit status
        if: always()
        uses: actions/github-script@v7
        env:
          STATE: ${{ steps.summarize.outputs.state || 'pending' }}
          DESCRIPTION: ${{ steps.summarize.outputs.description || 'Gate status pending' }}
          TARGET_URL: ${{ github.server_url }}/${{ github.repository }}/actions/runs/${{ github.run_id }}
        with:
          script: |
            const sha = context.payload.pull_request?.head?.sha ?? context.sha;
            const rawState = process.env.STATE || 'pending';
            const allowed = new Set(['error', 'failure', 'pending', 'success']);
            const state = allowed.has(rawState) ? rawState : 'pending';
            const description = (process.env.DESCRIPTION || 'Gate status update').slice(0, 140);
            try {
              await github.rest.repos.createCommitStatus({
                owner: context.repo.owner,
                repo: context.repo.repo,
                sha,
                state,
                context: 'Gate / gate',
                description,
                target_url: process.env.TARGET_URL,
              });
            } catch (error) {
              if (error?.status === 403 && /rate limit/i.test(error?.message || '')) {
                core.warning('Rate limit prevented Gate from updating commit status.');
              } else {
                throw error;
              }
            }

      - name: Enforce Gate success
        if: steps.summarize.outputs.state == 'failure'
        run: exit 1
